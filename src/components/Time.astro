---
const { time, modified } = Astro.props;
---

<span class="inline-block">
  {`${modified ? "Last Updated" : "Published"} : `}
  <time dateTime={`${time}Z`}>
    {new Date(`${time}Z`)
      .toDateString()
      .split(" ")
      .map((s, i) => (i === 1 || i === 3 ? s : s + ","))
      .join(" ")}
  </time>
</span>
