<strong>
  <slot />
  {Array.from({ length: 3 }).map(() => {
    const top = Math.random() * 100;
    const left = Math.random() * 100;
    const length = Math.random() / 2 + 0.25;

    return (
      <svg
        viewbox="0 0 143 137"
        class="star absolute transition-transform duration-700"
        style={`top: ${top}%; left: ${left}%; width: ${length}rem; height: ${length}rem;`}
      >
        <path d="m55 54-6 41-8-40-41-6 41-8 5-41 8 41 41 5Z" fill="#ffd42a"/>
      </svg>
    );
  })}
</strong>